if kode == nil then return end

local Event = require "app.config.events"

local AirBattleService = BaseService:extend()
local meta = AirBattleService

function meta:reqFunction()
	-- body
end
function meta:onFunction(param)
	-- body
end

return meta
